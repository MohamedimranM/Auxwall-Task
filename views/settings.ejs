<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Settings</title>
  <link rel="stylesheet" href="/css/settings.css">

</head>
<body>
  <form action="/settings" method="POST">
    <h1>Settings</h1>
    <label for="maxExpiryTime">Maximum Expiry Time (days):</label>
    <input type="number" id="maxExpiryTime" name="maxExpiryTime" value="<%= settings.maxExpiryTime || '' %>" required>

    <label for="voucherWidth">Voucher Width (mm):</label>
    <input type="number" id="voucherWidth" name="voucherWidth" value="<%= settings.voucherWidth || '' %>" required>

    <label for="voucherHeight">Voucher Height (mm):</label>
    <input type="number" id="voucherHeight" name="voucherHeight" value="<%= settings.voucherHeight || '' %>" required>

    <label for="titleFontSize">Title Font Size (px):</label>
    <input type="number" id="titleFontSize" name="titleFontSize" value="<%= settings.titleFontSize || '' %>" required>

    <label for="textFontSize">Text Font Size (px):</label>
    <input type="number" id="textFontSize" name="textFontSize" value="<%= settings.textFontSize || '' %>" required>

    <button type="submit">Save Settings</button>
  </form>
</body>
</html>
